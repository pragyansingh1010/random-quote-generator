# Quote Rendering

A new quote should replace both the quote text and its author together. Empty or malformed API responses should produce a readable fallback instead of leaving stale content on screen.