# Failure States

Network failures, empty responses, and malformed payloads should display a useful fallback. The retry action should request a fresh quote without duplicating stale loading indicators.