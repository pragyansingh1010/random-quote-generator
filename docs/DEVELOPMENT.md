# Development Notes

Keep quote data separated from rendering. Loading states, empty states, and failed requests should be represented explicitly so the interface never appears frozen during a fetch.
