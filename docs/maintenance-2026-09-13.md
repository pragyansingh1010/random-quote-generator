# Maintenance Notes

- Keep quote rendering readable on small screens.
- Handle failed quote retrieval without breaking the interface.
- Avoid repeating the same quote when alternatives are available.
- Keep loading feedback visible during retrieval.